These are the `not`, `and` and `or`. The first takes only 1 operand (unary) whereas the others take 2 (binary). They'll evaluate the operands in terms of [[Truthiness]].

They allow for [[Short circuit]] evaluations.
