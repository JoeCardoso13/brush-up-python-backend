**Mappings** represent an **unordered** [[Collection]] of [[Object]]s stored as key-value pairs. Each key (usually a [[String]]) provides a unique identifier for a specific [[Object]] in the mapping. The value is the [[Object]] associated with that key.

The [[Dictionary]] (class `dict`) is the most common mapping in Python.
