You can also reverse [[List]]s using the `list.reverse` [[Method]]. However, this [[Method]] mutates the [[List]].
```python
names = ('Grace', 'Clare', 'Allison', 'Trevor')
print(names.reverse())   # None
print(names) # ['Trevor', 'Allison', 'Clare', 'Grace']
```
