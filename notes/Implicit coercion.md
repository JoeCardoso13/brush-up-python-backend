Sometimes Python transforms one [[Type]] into another without an explicit [[Function call]], it does it implicitly.

* The `print` [[Function]] does an implicit coercion of its argument to [[String]] every time it's called.
* [[Boolean]]s in arithmetic operations are coerced to `0` or `1` if they are `False` or `True` respectively.
