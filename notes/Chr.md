`chr` is the inverse of `ord`. That is, it converts an integer to the corresponding Unicode character:
```python
print(chr(97))                # a
print(chr(65))                # A
print(chr(61))                # =
print(chr(126))               # ~
```
